package main

import (
	//"flag"
	"fmt"
	common "github.com/ajangi/gogit/src/common"
)

func main(){
	common.Intro();
	/* helpFlag := flag.String("help","","The help flag.")
	flag.Parse()
	if *helpFlag == "" {
        flag.PrintDefaults()
        os.Exit(1)
    }
	fmt.Printf("help: %s \n", *helpFlag, ) */
}